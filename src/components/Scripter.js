import React from 'react';
import styled from 'styled-components';

const ScripterBox = styled.div`
    flex-grow: 1;
    background: #fff;
    border-radius: 16px;
    border: 1px solid #000;
    margin: 0 0 0 1rem;
    padding: 1rem;
`

const Scripter = () => {
    return (
        <ScripterBox>scripter</ScripterBox>
    );
};

export default Scripter;